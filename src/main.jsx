import React from "react";
import ReactDOM from "react-dom/client";
import "./style.css";
import { PokemonExpertApp } from "./PokemonExpertApp";

ReactDOM.createRoot(document.getElementById("root")).render(
   // <React.StrictMode>
      <PokemonExpertApp />
   // </React.StrictMode>
);
